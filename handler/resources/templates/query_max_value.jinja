select MAX({{column}}) as max_value
from
    {{table}}
where
    1=1
    {% if equal_params %}
        {% for key, value in equal_params.items() %}
            and {{key}} = {{value}}
        {% endfor %}
    {% endif %}
